# 🧱 Edge Detection – Computer Vision Project

This project demonstrates multiple edge-detection techniques using **Streamlit**, **OpenCV**, and **PIL**.
Upload an image and visualize different edge detection outputs instantly.

## 🚀 Features

* **Laplacian (PIL Kernel)**
* **PIL FIND_EDGES**
* **Sobel (X, Y, XY)**
* **Canny Edge Detection**
* Simple and interactive **Streamlit UI**

## 📦 Installation

Create and activate a virtual environment:

```bash
python3 -m venv venv
source venv/bin/activate
```

Install required packages:

```bash
pip install -r requirements.txt
```

## ▶️ Running the App

```bash
streamlit run app.py
```

## 📁 Project Structure

```
project/
│── app.py
│── requirements.txt
│── .gitignore
```

## 📝 Description

This project is built to explore different edge detection algorithms in computer vision.
The UI allows users to compare multiple methods and understand how each technique highlights edges differently.

